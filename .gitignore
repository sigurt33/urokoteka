# Секреты — не публиковать
config.py

# Python
__pycache__/
*.pyc

# Временные/выходные файлы
*.mp4
out*.json

# Переносимые сборки (содержат config.py с ключом) — не публиковать
*.zip
portable/
portable*/

# Сборка .exe (PyInstaller) — содержит ключ, не публиковать
dist/
build/
*.spec
*.exe
context-local/
scr/

# Модуль автоматизации CRM — только локально
crm_*
